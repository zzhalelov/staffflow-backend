server:
  port: 8081

cors:
  allowed-origins: http://localhost:3000,http://localhost:8080,http://localhost:8081,http://127.0.0.1:5500,http://localhost:63342

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hrms-spring-jpa
    username: postgres
    password: postgres
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
  mail:
    host: smtp.gmail.com
    port: 587
    username: zzhalelov@gmail.com
    password: jsng gfpy zbkc wotk
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: false

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus

  endpoint:
    prometheus:
      enabled: true
